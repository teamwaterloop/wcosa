# Cosa Toolchain
set(CMAKE_TOOLCHAIN_FILE "{{WIO_PATH}}/toolchain/cmake/CosaToolchain.cmake")
cmake_minimum_required(VERSION 3.0.0)
project({{library-name}} C CXX ASM)

file(GLOB_RECURSE SRC_FILES "{{lib-path}}/src/*.cpp" "{{lib-path}}/src/*.cc" "{{lib-path}}/src/*.c")
generate_arduino_library({{library-name}}
	SRCS ${SRC_FILES}
	BOARD {{board}})
target_compile_definitions({{library-name}} PRIVATE __AVR_{{framework}}__ {{compile-flags}})
include_directories("{{lib-path}}")
